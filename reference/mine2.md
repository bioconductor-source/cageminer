# Example output from mine_step2()

The list was created using the example code from mine_step().

## Usage

``` r
data(mine2)
```

## Format

List with elements 'candidates' (character vector) and 'enrichment'
(data frame).

## Examples

``` r
data(mine2)
```
